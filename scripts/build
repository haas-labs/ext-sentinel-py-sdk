#!/bin/bash -e

source scripts/common

SENTINEL_VERSION=$(setuptools-git-versioning)
echo ${SENTINEL_VERSION} > src/sentinel/VERSION

${PREFIX}python3 -m build

# ${PREFIX}python setup.py sdist bdist_wheel
# ${PREFIX}twine check dist/*
# ${PREFIX}mkdocs build