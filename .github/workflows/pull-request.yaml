name: pull-request

on:
  pull_request:
    branches: [ "main" ]

jobs:
  build-on-pull-request:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
      with:
        fetch-depth: 0
