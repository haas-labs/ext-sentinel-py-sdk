sentries:

- name: eth://UserLoggerSentry
  type: sentry.logger.UserLoggerSentry
  parameters:
    network: ethereum
  inputs:
  - local/fs/transaction
  outputs:
  - local/fs/vent
  databases:
  - local/address
